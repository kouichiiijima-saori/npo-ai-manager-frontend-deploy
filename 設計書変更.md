#### 変更前

OrganizationProfile の主な保持項目

団体名

法人種別

設立年月日

代表者名

住所

団体概要

活動目的

#### 変更後

OrganizationProfile の主な保持項目

団体名

代表者名

所在地

設立年月日

活動地域

ミッション

対象者

主な活動

#### 理由

PG-A03 団体基本情報管理のフロント実装項目に合わせるため。
MVPではフロント完成済み項目を優先し、API・Domain・DBを同じ項目名で揃える。

---

# 7. grant_masters 変更

変更前
grant_master_id BIGINT NOT NULL,

変更後
CREATE TABLE grant_cases (
id BIGINT PRIMARY KEY AUTO_INCREMENT,

    organization_id BIGINT NOT NULL,

    grant_master_id BIGINT NOT NULL,

    case_name VARCHAR(255) NOT NULL,

    case_stage VARCHAR(50) NOT NULL,

    examination_status VARCHAR(50) NOT NULL,

    external_audit_status VARCHAR(50) NOT NULL,

    examination_memo TEXT,

    next_action TEXT,

    next_action_due_date DATE,

    is_archived BOOLEAN NOT NULL DEFAULT FALSE,

    archived_at DATETIME,

    archive_reason TEXT,

    created_at DATETIME NOT NULL,

    updated_at DATETIME NOT NULL,
